aws cli initialization template for serverless app with **AWS Lambda, Amazon API Gateway, Amazon S3 and Amazon DynamoDB**

```shell
$ docker-compose up -d
$ chmod +x localstack/init.sh && localstack/init.sh
```